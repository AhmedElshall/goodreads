import React from "react";

const rating = props => {
  let i=props.rate;
  for(; i >=1 ; i-- )  {

  }
  if( i > 0 )

  for(i=5-props.rate; i >=1 ; i-- )  {

  }
  
  return <>
  </>;
};

export default rating;
